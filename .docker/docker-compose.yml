version: '3.7'

services:
  minimalism-module-jsonapi-web:
    container_name: minimalism-module-jsonapi-web
    image: carlonicora/php-cli-base:7.4.3.2
    working_dir: /app
    volumes:
      - ../:/app
      - ../:/opt/project
    tty: true
    stdin_open: true